@using Frontend
@page "/"
@namespace Frontend.Components.Pages
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        Navigation.NavigateTo(
            DevAuthBypass.SkipLogin ? "/home" : "/login",
            forceLoad: true);
    }
}
